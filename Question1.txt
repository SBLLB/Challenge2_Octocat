

When you git clone you are essentially copying an existing remote repository, typically it’s a remote repository being copied to your local environment.

When you clone a repo only the local, master branch is copied. 